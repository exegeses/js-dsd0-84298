const cadena = 'cualquier tecnología lo suficientemente avanzada es indistinguible de la magia'

const sub = 'magia'

//let resultado = cadena.includes(sub)
let resultado = (cadena.includes(sub)) ? 'Lo contiene': 'No lo contiene'

console.log( resultado )
